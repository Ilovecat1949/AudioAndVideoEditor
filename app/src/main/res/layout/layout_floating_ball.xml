<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <LinearLayout
        android:id="@+id/cardContainer"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_floating_ball"
        android:paddingStart="12dp"
        android:paddingEnd="12dp">

        <ImageView
            android:id="@+id/btnControl"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:padding="4dp"
            android:src="@drawable/play_circle_24px"
            android:contentDescription="@string/desc_control_button"
            android:background="?android:attr/selectableItemBackgroundBorderless" />

        <!-- 2. 停止录制并保存 按钮（默认在待机时隐藏 GONE） -->
        <ImageView
            android:id="@+id/btnStop"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginStart="4dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/stop"
            android:padding="6dp"
            android:src="@drawable/stop_circle_24px"
            android:visibility="gone" />


        <View
            android:layout_width="1dp"
            android:layout_height="18dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:background="#40FFFFFF" />

        <ImageView
            android:id="@+id/btnClose"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:padding="4dp"
            android:src="@drawable/baseline_close_24"
            android:tint="#FFFFFF"
            android:contentDescription="@string/desc_close_floating_ball"
            android:background="?android:attr/selectableItemBackgroundBorderless" />

    </LinearLayout>
</FrameLayout>